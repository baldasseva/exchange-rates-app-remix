import React from "react";
import { Center, Loader } from "@mantine/core";

const Spinner = () => {
  return (
    <Center>
      <Loader color="orange" />
    </Center>
  );
};

export default Spinner;
